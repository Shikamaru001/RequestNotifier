name: Deploy

on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install Heroku CLI
        run: |
          curl https://cli-assets.heroku.com/install.sh | sh 
      - uses: akhileshns/heroku-deploy@v3.14.15
        with:
          heroku_api_key: HRKU-AAulL0IqjemAavaIfjhRSIo90Vpou0RNqkfQBJh7Si2g_____whH9f9uNFiQ
          heroku_app_name: aaaommmm
          heroku_email: VikramSingh926339@outlook.com
          team: vastusastrasam92379
          usedocker: true
          docker_heroku_process_type: worker
          stack: "container"
          region: "eu"
